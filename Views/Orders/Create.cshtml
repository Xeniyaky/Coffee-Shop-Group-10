@model CoffeeShopMVC.Models.Order

<section class="card p-4 shadow-sm mx-auto" style="max-width: 600px; margin-top: 50px;">
    <h3>Enter Order Information</h3>
    <form asp-action="Create" method="post">
        <div class="mb-3">
            <label class="form-label fw-bold">Customer Name</label>
            <input asp-for="CustomerName" class="form-control" placeholder="Enter name" required />
        </div>

        <label class="form-label fw-bold">Items</label>
        <div id="items-container">
            <div class="row g-2 mb-2 item-row">
                <div class="col-6">
                    <input name="DrinkItems" class="form-control" placeholder="Drink Name" required />
                </div>
                <div class="col-3">
                    <select name="Sizes" class="form-select">
                        <option value="Small">Small</option>
                        <option value="Medium">Medium</option>
                        <option value="Large">Large</option>
                    </select>
                </div>
                <div class="col-3">
                    <input name="Quantities" type="number" class="form-control" placeholder="Qty" min="1" value="1" />
                </div>
            </div>
        </div>

        <div class="mt-3 d-flex justify-content-between">
            <button type="button" class="btn btn-outline-secondary btn-sm" onclick="addItem()">+ Add Another Item</button>
            <button type="submit" class="btn btn-brown">Submit Full Order</button>
        </div>
    </form>
</section>

@section Scripts {
    <script>
        function addItem() {
            const container = document.getElementById('items-container');
            const rows = document.querySelectorAll('.item-row');
            const newRow = rows[0].cloneNode(true);

            // Clear the values in the cloned row so it doesn't copy the text
            newRow.querySelectorAll('input').forEach(input => input.value = '');
            newRow.querySelector('select').selectedIndex = 0;

            container.appendChild(newRow);
        }
    </script>
}